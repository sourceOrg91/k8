apiVersion: v1
kind: Service
metadata:
  name: dp-maxwell-service
spec:
  selector:
    k8s-app: dp-maxwell
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
